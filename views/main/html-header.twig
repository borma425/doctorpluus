

 <!-- navbar -->
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container">
            <a class="navbar-brand w-25" href="{{ site.link }}"><img src="{{ logo_100_x_100 }}"
                    class="navbar-img w-50" alt="logo"></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false"
                aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0 fs-5">
          {% for item in header_menu_1.items %}

                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ item.link|e('esc_html') }}">{{ item.title }}</a>
                    </li>
{% endfor %}

                </ul>
            </div>
        </div>
    </nav>